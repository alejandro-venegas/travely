import { InputMaskDirective } from './input-mask.directive';

describe('InputMaskWrapperDirective', () => {
  it('should create an instance', () => {
    const directive = new InputMaskDirective();
    expect(directive).toBeTruthy();
  });
});
