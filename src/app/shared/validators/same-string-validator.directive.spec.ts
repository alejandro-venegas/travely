import { SameStringValidatorDirective } from './same-string-validator.directive';

describe('SameStringValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new SameStringValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
