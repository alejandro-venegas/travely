import { UsedStringValidatorDirective } from './used-string-validator.directive';

describe('UsedStringValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new UsedStringValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
